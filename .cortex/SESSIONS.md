# Sessions

## 2026-05-01 - Codex Cortex setup and system install

- Changed: redesigned the template as `codex-cortex`, added GitHub-ready docs, installed Cortex into the repo itself, and created a global Codex skill.
- Verified: PowerShell scripts parse, JSON metadata parses, template validation reports no missing files, and skill validation passes.
- Not done: GitHub publication did not complete because `gh` is unavailable and the GitHub connector has no installed accounts/repositories.
- Status: local git repo exists with initial commit; root Cortex install still needs commit after this state update.
- Follow-up: resolve GitHub access, commit the root Cortex install, push to GitHub, choose license.
